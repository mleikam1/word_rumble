name: word_rumble
description: "Word Rumble — an arcade physics word game built with Flutter + Flame."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Ads
  google_mobile_ads: ^5.1.0

  # Game engine
  flame: ^1.17.0

  # Optional advanced physics engine
  flame_forge2d: ^0.19.0+4

  # App state (coins, power-ups, progression)
  provider: ^6.1.0

  # UI icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/audio/
    - assets/levels/
